import 'package:flutter/material.dart';

// Step 3: Utility class for reusable color constants
class AppColors {
  // Primary dark blue used in buttons, AppBar, and accents
  static const Color primary = Color(0xFF1D3461);

  // Light blue used for links and highlights
  static const Color accent = Color(0xFF2979FF);

  // White background for all screens
  static const Color background = Colors.white;

  // Light grey for input field borders
  static const Color inputBorder = Color(0xFFB0BEC5);

  // Grey for subtitle/hint text
  static const Color subtitle = Color(0xFF757575);

  // Error red for validation messages
  static const Color error = Colors.red;
}